public class PuzzleGame {

    /*

   private final EASY_GIVEN_CELLS_AMOUNT;
   private final MEDIUM_GIVEN_CELLS_AMOUNT;
   private final HARD_GIVEN_CELLS_AMOUNT;
   private final N;
   private int[][] puzzle;

    ENUM difficulty {Easy, Medium, Hard}

    init( ENUM difficulty)

        case EASY:
            generate EASY_GIVEN_CELLS_AMOUNT;
        case MEDIUM:
          generate MEDIUM_GIVEN_CELLS_AMOUNT;
        case HARD:
            generate HARD_GIVEN_CELLS_AMOUNT;
/*

fill the boolean array of 81 with false
initialize level-constnt elements to T
shuffle array
than iterate and set the T to a random number


   public generate(
          for(each row && row> 0 && row < N){

            for(each col row> 0 && row < N){
                generate new Random(1-9) number
                while (row.include(random) || column.includes(random)
                        && box.includes(random){
                   generate new Random(1-9) number
                   set random number in the grid[ran][ran]
                }

                set Random(1-9) number
            }
          }


          public getCellValue( int row, int col){
                validate(row, col)
                return grid[row][col];
          }

          public setSetValue(int row, int col){


          }

          public validate( int row, int col, int val){
            if (row >=N ||  row <=0 || col >=N ||  col <=0 || val <=0 || val >=N){
                // notify user about invalid argument
            }

          }


          public checkSubgrid(

     */

}
